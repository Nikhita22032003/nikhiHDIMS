<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.health">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Health"
        tools:targetApi="31">

        <!-- SplashActivity is the launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Health">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ImageSliderAdapter"
            android:exported="true"> <!-- ✅ REQUIRED FOR ANDROID 12+ -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- MainActivity (navigated to after splash) -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:label="health"
            android:theme="@style/Theme.Health" />

        <!-- ✅ Add LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Login" />

        <!-- ✅ Add RegisterActivity if needed -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:label="Register" />
        <activity
            android:name=".DashboardActivity"
            android:exported="true"
            android:label="Dashboard"
            android:theme="@style/Theme.Health" />

        <activity android:name=".HealthRecordsActivity"/>
        <activity android:name=".AppointmentsActivity"/>







    </application>
</manifest>
